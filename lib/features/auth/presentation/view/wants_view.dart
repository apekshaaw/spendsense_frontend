import 'package:flutter/material.dart';

class WantsView extends StatelessWidget {
  const WantsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Wants'),
      ),
      body: const Center(
        child: Text('Wants page – coming soon'),
      ),
    );
  }
}
